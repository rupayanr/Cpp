   void selectionSort()
    //     {

    //         /* Finding the min element of unsorted array first
    //     and swapping its position with the first element of sorted array*/

    //         int min_element = 0;

    //         for (int i = 0; i < (len - 1); i++)
    //         {
    //             for (int j = i + 1; j < len; j++)
    //             {

    //                 if (arr[j] < arr[i])
    //                 {
    //                     min_element = arr[j];
    //                     arr[j] = arr[i];
    //                     arr[i] = min_element;
    //                 }
    //             }
    //         };

    //         cout << "Sorted Array: ";
    //         display();
    //     };
    // };